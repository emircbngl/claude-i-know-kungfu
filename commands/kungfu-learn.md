---
description: Record a mistake as a misconception → correct-model lesson, recalled next time.
argument-hint: [what went wrong → what is correct]
---

Read the `i-know-kungfu` skill and `references/learning-capture.md`.

From the conversation (and $ARGUMENTS), capture the lesson with `kungfu_learn`:
- `misconception` (the wrong belief), `trigger` (when this wrong thought occurs — the recall key), `wrong`, `right`, and `model` (the structural reason).
- `source`: `verify-failure` if a compiler proved it, else `user-correction`; set `verified=true` only if the `right` actually compiled.

Confirm what was stored and whether it was folded into the language skeleton.
