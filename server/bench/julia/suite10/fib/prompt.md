# Task: Fibonacci number

In `src/sol.jl`, implement `fib(n)` that returns the `n`-th Fibonacci number as
an `Int`, with `fib(0) = 0` and `fib(1) = 1`.

Signature: `function fib(n)` (where `n` is a non-negative `Int`).

Examples: `fib(0)` = 0, `fib(1)` = 1, `fib(10)` = 55, `fib(20)` = 6765.
