# Task: maximum of a list

Implement `pub fn max(items: List(Int)) -> Int` in `src/list_max.gleam` that
returns the largest integer in the list (assume the list is non-empty and all
values are >= 0). Use a fold from `gleam/list`.
