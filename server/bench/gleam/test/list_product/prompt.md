# Task: product of a list

Implement `pub fn product(items: List(Int)) -> Int` in `src/list_product.gleam`
that returns the product of the integers (1 for an empty list). Use a fold from
`gleam/list`.
