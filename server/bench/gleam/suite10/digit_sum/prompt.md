# Task: sum of digits

Implement a function that returns the sum of the decimal digits of a
non-negative integer `n`.

In `src/sol.gleam` (module `sol`), implement:

```gleam
pub fn digit_sum(n: Int) -> Int
```

Examples: `digit_sum(1234)` = 10, `digit_sum(0)` = 0, `digit_sum(99)` = 18.
