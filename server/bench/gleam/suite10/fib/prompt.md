# Task: Fibonacci number

Implement a function that returns the `n`-th Fibonacci number, where
`fib(0) = 0` and `fib(1) = 1`.

In `src/sol.gleam` (module `sol`), implement:

```gleam
pub fn fib(n: Int) -> Int
```

Examples: `fib(0)` = 0, `fib(1)` = 1, `fib(10)` = 55, `fib(20)` = 6765.
