# Task: reverse a list

Implement `pub fn reverse(items: List(a)) -> List(a)` in
`src/list_reverse.gleam` that returns the list with its elements in reverse
order. Prefer the standard library (`gleam/list`).
