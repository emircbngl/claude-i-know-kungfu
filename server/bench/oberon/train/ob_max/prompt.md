# Task: maximum of the first n elements

In module `Sol` (file `src/Sol.obn`), implement
`PROCEDURE Max*(a: ARRAY OF INTEGER; n: INTEGER): INTEGER` returning the largest
of `a[0]` .. `a[n-1]` (n >= 1).
