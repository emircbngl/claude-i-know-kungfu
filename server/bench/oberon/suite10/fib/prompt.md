# Task: Fibonacci number

In a module named `Sol` (file `src/Sol.obn`), implement

```
PROCEDURE Fib*(n: INTEGER): INTEGER
```

returning the `n`-th Fibonacci number, with `Fib(0) = 0` and `Fib(1) = 1`.

Examples: `Fib(0)` = 0, `Fib(1)` = 1, `Fib(10)` = 55, `Fib(20)` = 6765.
