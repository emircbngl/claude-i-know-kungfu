# Task: sum of decimal digits

In a module named `Sol` (file `src/Sol.obn`), implement

```
PROCEDURE DigitSum*(n: INTEGER): INTEGER
```

returning the sum of the decimal digits of `n`. You may assume `n >= 0`.

Examples: `DigitSum(1234)` = 10, `DigitSum(0)` = 0, `DigitSum(99)` = 18.
