MODULE Sol;

  PROCEDURE DigitSum*(n: INTEGER): INTEGER;
    VAR s: INTEGER;
  BEGIN
    s := 0;
    WHILE n > 0 DO
      s := s + n MOD 10; n := n DIV 10
    END
    RETURN s
  END DigitSum;

END Sol.
